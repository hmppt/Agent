# 🚀 快速启动卡片

## 方式 1：一键启动（推荐）

```bash
# 在项目根目录执行
./start-all.sh
```

**自动完成**：
- ✅ 检查端口占用
- ✅ 启动后端服务（8000）
- ✅ 启动前端服务（5173）
- ✅ 打开浏览器访问

**停止服务**：
```bash
./stop-all.sh
```

---

## 方式 2：手动启动

### 终端 1 - 后端
```bash
cd backend
./start.sh
```

### 终端 2 - 前端
```bash
cd frontend
npm run dev
```

---

## 📍 访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| **前端应用** | http://localhost:5173 | 聊天界面 |
| **后端 API** | http://localhost:8000 | API 服务 |
| **API 文档** | http://localhost:8000/docs | Swagger UI |
| **健康检查** | http://localhost:8000/health | 服务状态 |

---

## ⚠️ 首次运行检查清单

- [ ] Python 版本 >= 3.14.0
- [ ] 已配置 `backend/.env` 文件
- [ ] API Key 有效且有余额
- [ ] 运行过 `cd backend && uv sync`
- [ ] 运行过 `cd frontend && npm install`

---

## 🔍 快速测试

### 1. 测试后端
```bash
curl http://localhost:8000/health
# 预期: {"status":"healthy"}
```

### 2. 测试前端
浏览器访问 http://localhost:5173，发送消息：
```
你好
```

应该看到 AI 逐字回复。

---

## 🐛 常见问题

### 端口被占用？
```bash
# 查看占用进程
lsof -i :8000
lsof -i :5173

# 强制停止
lsof -ti :8000 | xargs kill -9
lsof -ti :5173 | xargs kill -9
```

### API 连接失败？
1. 检查后端是否运行
2. 查看后端日志：`tail -f /tmp/agent-backend.log`
3. 确认 `.env` 中的 API Key 正确

### 前端报错？
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

---

## 📚 详细文档

完整启动指南：[STARTUP.md](./STARTUP.md)

---

**提示**：后端启动时会看到 `Agent ready!` 表示初始化成功。
